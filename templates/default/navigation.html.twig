<nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_landingpage') }}">FalschParkerZähler</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex ms-auto">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link ms-2 me-2" href="{{ path('app_home') }}"><i class="bi bi-person-vcard"></i> Zähler</a> |
                    {% if is_granted('ROLE_SUPERADMIN') %}
                    <a class="nav-link ms-2 me-2" href="{{ path('admin') }}" target="_blank"><i class="bi bi-person-circle"></i> Admin</a> |
                    {% endif %}
                    <a class="nav-link ms-2 me-2" href="{{ path('app_logout') }}"><i class="bi bi-person-circle"></i> LogOut</a> |
                    <span class="ms-2">{{ app.user.firstname ~ ' ' ~ app.user.lastname }}</span>
                {% else %}
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">LogIn</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Registrieren</a></li>
                    </ul>
                {% endif %}
            </div>
        </div>
    </div>
</nav>